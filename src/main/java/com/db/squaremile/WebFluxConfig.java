package com.db.squaremile;

import org.springframework.context.annotation.Configuration;

//@Configuration
//@EnableWebFlux
public class WebFluxConfig {
	/*
	 * @Bean public WebClient webClient() { ConnectionProvider connectionProvider =
	 * ConnectionProvider.builder("connection-pool").maxConnections(100000)
	 * .pendingAcquireMaxCount(10000).build(); ReactorClientHttpConnector
	 * clientHttpConnector = new ReactorClientHttpConnector(
	 * HttpClient.create(connectionProvider)); WebClient webClient =
	 * WebClient.builder().clientConnector(clientHttpConnector).baseUrl(
	 * "http://localhost:8087") .defaultCookie("cookie-name", "cookie-value")
	 * .defaultHeader(HttpHeaders.CONTENT_TYPE,
	 * MediaType.APPLICATION_JSON_VALUE).build(); return webClient; }
	 */

}
